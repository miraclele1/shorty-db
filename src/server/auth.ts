/**
 * Better Auth configuration.
 *
 * TODO: configure after Neon database is set up and Google OAuth credentials
 * are added to .env.
 *
 * Will provide:
 *   - Google OAuth provider
 *   - Email Magic Link via Resend
 *   - Session management in Postgres (via Drizzle adapter)
 *
 * Docs: https://www.better-auth.com/docs
 */

export const auth = null;
